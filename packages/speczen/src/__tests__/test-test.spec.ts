import * as _ from 'lodash'

_.times(1, (i) => {
  describe(`dummy-test-test.spec.ts`, () => {
    describe(`resultSpecFromTitles`, () => {
      it(`should`, async () => {
        // ERROR to match!
        // _log('will error', expected)
        expect(true).toBe(true)
      })
    })
  })
})
